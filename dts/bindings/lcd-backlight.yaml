
description: GPIO pin to switch a lcd-backlight

compatible: "lcd-backlight"

properties:
  gpios:
    type: phandle-array
    required: true
    description: |
      The GPIO connected to the LCD backlight pin
